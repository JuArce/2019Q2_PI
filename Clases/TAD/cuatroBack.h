

#define DIM 
struct estructura
{
	char tablero[DIM][DIM];
	char jugador1[MAX];
	char jugador2[MAX];
	int cantJugadores;
	int nivel;
};

int jugar(struct estructura * tablero, );

int deshacer(...);

int jugador1(,char *nombre);
